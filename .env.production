# This file is used by Vite at build time on Render.
# Render injects VITE_API_BASE_URL as a build-time env var from render.yaml —
# so you do NOT need to commit secrets here.
VITE_API_BASE_URL=https://rentsphere-backend-7m46.onrender.com/api/v1
